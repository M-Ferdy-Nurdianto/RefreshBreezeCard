{
  "name": "rb-card-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build-all && npm run consolidate",
    "build-all": "npm run build-aca && npm run build-channie && npm run build-cissi && npm run build-piya",
    "build-aca": "cd \"aca card\" && npm install && npm run build",
    "build-channie": "cd \"channie card\" && npm install && npm run build",
    "build-cissi": "cd \"cissi card\" && npm install && npm run build",
    "build-piya": "cd \"piya card\" && npm install && npm run build",
    "consolidate": "shx rm -rf public && shx mkdir -p public/aca public/channie public/cissi public/piya && shx cp index.html style.css logo.webp public/ && shx cp -r \"aca card/dist/\"* public/aca/ && shx cp -r \"channie card/dist/\"* public/channie/ && shx cp -r \"cissi card/dist/\"* public/cissi/ && shx cp -r \"piya card/dist/\"* public/piya/"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
